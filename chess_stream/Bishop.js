class Bishop extends Piece
{
	constructor(){
		super("bishop");
	}

	/*getLegalMoves(board)
	{	
		this.legalMoves = [];
		for(let i = this.tile.tileY; i >= 0; i++){
			for(let j = this.tile.tileX; j < 8 - this.tile.tileX; j++){
				this.legalMoves.push(board.tiles[this.tile.tileY + i][this.tile.tileX + j]);
			}
		}

		for(let i = 0; i < 8; i++){
			for(let j = 0; j < 8; j++){

			}
		}

		for(let i = 0; i < 8; i++){
			for(let j = 0; j < 8; j++){

			}
		}

		for(let i = 0; i < 8; i++){
			for(let j = 0; j < 8; j++){

			}
		}
	}*/

}